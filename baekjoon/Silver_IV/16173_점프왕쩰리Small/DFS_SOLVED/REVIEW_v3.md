# 백준 16173번: 점프왕 쩰리 (Small) 리뷰 (v3)

| 구분 | 내용 |
|:---:|:---|
| 파일 | P16173_v3.java |
| 날짜 | 2026-01-27 |
| 알고리즘 | `DFS (스택 기반)` |
| 자료구조 | `Stack`, `2차원 배열` |
| 시간복잡도 | O(N²) |
| 공간복잡도 | O(N²) |

**개선된 점 (v2 대비)**
- 스택 기반 DFS로 제대로 구현 ✓
- while 루프로 스택 탐색 ✓
- 경계 조건 수정 (`>= 0`, `< N`) ✓
- 방문 체크를 push 전에 수행 ✓

**잘한 점**
- 스택 기반 DFS 구조가 깔끔해짐
- 경계 조건 올바르게 수정
- 코드 구조 개선

**개선할 점**

1. **아직 대각선 이동!**
   - 왜? 53-55번 줄에서 for문은 2번 도는데, 매번 같은 계산을 함
   - `next_row = now_row + move`, `next_col = now_col + move` → 둘 다 동일
   - 이렇게 바꿔야 함:
     - i=0일 때: 오른쪽 이동 → `(now_row, now_col + move)`
     - i=1일 때: 아래 이동 → `(now_row + move, now_col)`
