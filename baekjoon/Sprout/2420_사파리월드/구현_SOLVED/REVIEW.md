# 백준 2420번: 사파리월드 리뷰

---

## v1 (2026-01-05) - P2420.java

| 구분 | 내용 |
|:---:|:---|
| 알고리즘 | `수학 (절댓값)` |
| 자료구조 | `없음` |
| 시간복잡도 | O(1) |
| 공간복잡도 | O(1) |

**잘한 점**
- `long` 타입을 사용하여 오버플로우 방지 (N, M 범위: -20억 ~ 20억)
- `Math.abs()`를 활용한 간결한 절댓값 계산
- `BufferedReader` + `StringTokenizer` 조합으로 효율적인 입력 처리

**개선할 점**

없음 - 문제의 핵심인 자료형 선택(`long`)과 절댓값 계산을 정확히 구현했습니다.

**핵심 포인트**
- 두 수의 차이가 최대 40억까지 가능하므로 `int` (약 21억)로는 부족
- `Long.parseLong()` 사용 필수
