# 백준 1330번: 두 수 비교하기 리뷰

---

## v1 (2025-01-04) - P1330.java

| 구분 | 내용 |
|:---:|:---|
| 알고리즘 | `조건문` |
| 자료구조 | `int` |
| 시간복잡도 | O(1) |
| 공간복잡도 | O(1) |

**잘한 점**
- BufferedReader + StringTokenizer 조합으로 빠른 입력 처리
- if-else if-else 구조로 모든 케이스 커버
- 변수명 A, B로 문제와 일치
- 간결하고 명확한 조건 분기

**개선할 점**
- 없음! 완벽한 코드

**배운 점**
- 기본적인 조건문 사용법
- Java에서 정수 비교는 `==`, `>`, `<` 연산자 사용
  - 주의: 객체(Integer) 비교 시에는 `.equals()` 사용 필요
- 이 문제는 기본 타입 `int`라서 `==` 사용 가능
